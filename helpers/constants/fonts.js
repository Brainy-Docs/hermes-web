import { Poppins, Inter, Montserrat, Lexend } from "next/font/google";

export const poppins = Poppins({
  weight: "400",
  subsets: ["latin"],
});

export const lexend = Lexend({
  weight: "400",
  subsets: ["latin"],
});

export const montserrat = Montserrat({
  weight: "400",
  subsets: ["latin"],
});
